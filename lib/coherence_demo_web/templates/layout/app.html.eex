<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="">
    <meta name="author" content="">

    <title>Hello CoherenceDemo!</title>
    <link rel="stylesheet" href="<%= static_path(@conn, "/css/app.css") %>">
  </head>

  <body>
    <div class="container">
      <header class="header" style="padding-top: 15px">
        <%= if Coherence.current_user(@conn) do %>
          <nav role="navigation">
            <ul class="nav nav-pills pull-left" style="margin-top: 0px">
              <li><a href="/posts">Posts</a></li>
              <li><a href="/users">Users</a></li>
            </ul>
          </nav>
        <% end %>
        <ol class="breadcrumb text-right">
          <%= if Coherence.current_user(@conn) do %>
            <%= if @conn.assigns[:remembered] do %>
              <li style="color: red;">!!</li>
            <% end %>
            <li><%= CoherenceDemoWeb.Coherence.ViewHelpers.invitation_link(@conn) %></li>
          <% end %>
          <%= CoherenceDemoWeb.Coherence.ViewHelpers.coherence_links(@conn, :layout) %>
        </ol>
      </header>

      <p class="alert alert-info" role="alert"><%= get_flash(@conn, :info) %></p>
      <p class="alert alert-danger" role="alert"><%= get_flash(@conn, :error) %></p>

      <main role="main">
        <%= render @view_module, @view_template, assigns %>
      </main>

    </div> <!-- /container -->
    <script src="<%= static_path(@conn, "/js/app.js") %>"></script>
  </body>
</html>
